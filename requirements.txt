# Core data stack
pandas>=2.0
numpy>=1.24
requests>=2.31

# Visualization
matplotlib>=3.7

# Econometrics / stats
statsmodels>=0.14
scikit-learn>=1.3
linearmodels>=5.3        # panel fixed-effects (PanelOLS) with clustered SEs

# Time-series forecasting
pmdarima>=2.0            # auto_arima (optional; statsmodels SARIMAX is the fallback)

# Alternative-data proxies
pytrends>=4.9            # Google Trends (unofficial; rate-limited)

# Reporting / convenience
python-dotenv>=1.0       # load API keys from .env
tabulate>=0.9           # markdown tables
